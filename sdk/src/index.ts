import { WalletClient } from "viem";
import { ClientType, createClient } from "./chain-client";

// Make the client, get commitment hub address, and create a signer
export function createScpContext(signer: WalletClient): {

} 
}
